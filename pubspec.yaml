name: lilypad
description: "Lilypad - Pixel Life Assistant"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0
  intl: ^0.20.2
  device_preview: ^1.3.1
  supabase_flutter: ^2.12.4
  google_sign_in: ^6.1.6
  http: ^1.6.0
  shared_preferences: ^2.5.5
  flutter_dotenv: ^5.1.0
  fl_chart: ^0.69.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
